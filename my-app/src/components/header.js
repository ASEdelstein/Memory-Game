import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Click on a Door!</h1>
    <div>
      </div>
    <h3 className="text-center">Click on a door to earn points, but don't click the same door twice!</h3>
  </div>
)
export default Header;